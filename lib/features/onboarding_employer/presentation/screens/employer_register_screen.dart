import 'package:flutter/material.dart';

// TODO: Implementar EmployerRegisterScreen
class EmployerRegisterScreen extends StatelessWidget {
  const EmployerRegisterScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EmployerRegisterScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
