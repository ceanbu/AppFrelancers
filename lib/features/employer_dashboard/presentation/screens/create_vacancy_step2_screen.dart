import 'package:flutter/material.dart';

class CreateVacancyStep2Screen extends StatelessWidget {
  const CreateVacancyStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Crear vacante - Detalles y habilidades')),
      body: const Center(
        child: Text('Formulario de detalles de la vacante (en construcción)'),
      ),
    );
  }
}
