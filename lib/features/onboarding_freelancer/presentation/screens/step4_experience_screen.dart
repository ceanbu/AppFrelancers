import 'package:flutter/material.dart';

// TODO: Implementar Step4ExperienceScreen
class Step4ExperienceScreen extends StatelessWidget {
  const Step4ExperienceScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step4ExperienceScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
