import 'package:flutter/material.dart';

// TODO: Implementar Step3SkillsScreen
class Step3SkillsScreen extends StatelessWidget {
  const Step3SkillsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step3SkillsScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
