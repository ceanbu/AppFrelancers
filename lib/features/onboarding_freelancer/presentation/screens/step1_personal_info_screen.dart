import 'package:flutter/material.dart';

// TODO: Implementar Step1PersonalInfoScreen
class Step1PersonalInfoScreen extends StatelessWidget {
  const Step1PersonalInfoScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step1PersonalInfoScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
