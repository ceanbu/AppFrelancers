import 'package:flutter/material.dart';

// TODO: Implementar Step2AvailabilityScreen
class Step2AvailabilityScreen extends StatelessWidget {
  const Step2AvailabilityScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step2AvailabilityScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
