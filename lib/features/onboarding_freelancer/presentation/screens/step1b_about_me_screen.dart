import 'package:flutter/material.dart';

// TODO: Implementar Step1bAboutMeScreen
class Step1bAboutMeScreen extends StatelessWidget {
  const Step1bAboutMeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Step1bAboutMeScreen')),
      body: const Center(child: Text('En construcción')),
    );
  }
}
